%a{:href => "/"} #{image_tag ("fsm_logo1.png"), :id => "logo_edit"}
.vcard-container
  .coda-slider-wrapper
    .layer
      = image_tag("http://graph.facebook.com/#{current_user.facebook_info.uid}/picture", :class => "avatar")
      %h1.name #{current_user.facebook_info.user_name} 
      %span#yourcard #{link_to "Your card" , card_landing_path(@user.slug), :popup => true}
      %br{:style => "clear:both;"}/
      #coda-slider-1.coda-slider.preload
        .panel
          .panel-wrapper.social
            .social-container
              %p.qrcode #{image_tag_qr_code(@user)}
              %br{:style => "clear:both;"}/
              %ul
                %li.twitter
                  %a{:href => "#"} Twitter
                %li.linkedin
                  %a{:href => "#"} Linkedin
                %li.lastfm.last-left
                  %a{:href => "#"} Last.fm
              %ul
                %li.facebook
                  %a{:href => "#"} Facebook
                %li.myspace
                  %a{:href => "#"} Myspace
                %li.flickr.last-right
                  %a{:href => "#"} Flickr
              %br{:style => "clear:both"}/



// %p #{@user.facebook_info.user_name}

// = @user.bio

// - if @user.facebook_info
//  = link_to_add_facebook_friend(@user)

- if @user.twitter_info
  = link_to_twitter_follow(@user)

- if @user.linked_in_info
  = link_to_linkedin(@user)

- if @user.last_fm_info
  = link_to_last_fm(@user)

- if @user.github_info
  = link_to_github(@user)

- if @user.foursquare_info
  = link_to_foursquare(@user)

- if @user.instagram_info
  = link_to_instagram(@user)

- if @user.tumblr_info
  = link_to_tumblr_follow(@user)

- if @user.flickr_info
  = link_to_flickr(@user)

- if @user.blog_url
  = link_to "Blog", @user.blog_url

